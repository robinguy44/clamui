# ClamUI CLI submodule
"""
Command-line interface components for ClamUI.

This module provides CLI entry points for headless operations:
- ``clamui scan`` — One-shot file/directory scanning
- ``clamui quarantine`` — Quarantine management (list/restore/delete)
- ``clamui profile`` — Scan profile management (list/show/export/import)
- ``clamui status`` — ClamAV and ClamUI health check
- ``clamui history`` — Scan history viewer
- ``clamui-scheduled-scan`` — Scheduled scan runner (systemd/cron)
"""
